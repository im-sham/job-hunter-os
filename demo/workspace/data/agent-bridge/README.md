Agent Bridge bundles will appear here when you prepare a handoff from the dashboard or CLI.

- `queue.yml` tracks recent local handoffs.
- `outbox/` contains per-task folders with prompts, manifests, and optional upload files.
- This folder stays local to the workspace so the user can choose how each assistant receives context.
